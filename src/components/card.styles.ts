import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  card: {
    width: "48%",
    minHeight: 125,
    padding: 15,
    borderRadius: 18,
  },
  icon: {
    fontSize: 24,
    marginBottom: 8,
  },
  title: {
    fontSize: 15,
    fontWeight: "800",
    marginBottom: 5,
  },
  description: {
    fontSize: 11,
    lineHeight: 16,
    opacity: 0.65,
  },
});
